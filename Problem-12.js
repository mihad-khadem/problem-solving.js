// Declare an array of mobile brands in bangladesh,Display all the elements of the array using *While* Loop.

var arr = ['Iphone', 'Samsung', 'Oppo', 'Walton', 'Infinix', 'Redmi','Realme'];
var i = 0;

while(i < arr.length){
    var items = arr[i]
    console.log(i + ' - ' +items);
    i++;
}